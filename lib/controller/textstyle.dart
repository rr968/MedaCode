import 'package:flutter/material.dart';
import '/controller/var.dart';

TextStyle textStyle11 = const TextStyle(fontSize: 15);
Gradient gradient = LinearGradient(
  begin: const Alignment(1.0, -1.0), // slightly outside top-right
  end: const Alignment(-1.0, 1.0), // slightly outside bottom-left
  colors: [deepOrange, yellow],
);
